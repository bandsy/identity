enum BandsyResponseCodes {
  ERROR_HANDLER_ERROR,
  UNKNOWN_ERROR,
  VALIDATION_ERROR,
  CLIENT_ERROR,
  SERVER_ERROR,
  DUPLICATE_EMAIL,
  INVALID_ACCOUNT,
}

export {
  // eslint-disable-next-line import/prefer-default-export
  BandsyResponseCodes,
};
